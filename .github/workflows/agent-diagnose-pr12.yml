name: Diagnose PR 12

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review]

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'agent/provider-packaging-cli-ci'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev]"
      - name: Capture mypy result
        shell: bash
        run: |
          set +e
          python -m mypy --show-error-context --show-error-codes --pretty \
            src/prob4d/cli.py \
            src/prob4d/provider_manifest.py \
            src/prob4d/provider_manifest_cli.py \
            > mypy-output.txt 2>&1
          status=$?
          printf 'mypy_status=%s\n' "$status" > status.txt
          cat status.txt
          cat mypy-output.txt
          exit 0
      - uses: actions/upload-artifact@v7
        with:
          name: pr12-diagnostics
          path: |
            status.txt
            mypy-output.txt
          retention-days: 2
