name: Apply DOT CUT3R marker-support v2 patch

on:
  push:
    branches:
      - "fix/dot-cut3r-marker-support-v2"
    paths:
      - ".github/workflows/apply-dot-cut3r-marker-support-v2.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out exact fix branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: fix/dot-cut3r-marker-support-v2
          fetch-depth: 0
          persist-credentials: true
          clean: true
      - name: Apply reviewed marker-support patch and remove helper workflow
        shell: bash
        run: |
          set -euo pipefail
          git apply --check <<'PATCH'
          diff --git a/scripts/science/run_dot_rope_cut3r_native_provider.py b/scripts/science/run_dot_rope_cut3r_native_provider.py
          --- a/scripts/science/run_dot_rope_cut3r_native_provider.py
          +++ b/scripts/science/run_dot_rope_cut3r_native_provider.py
          @@ -34,6 +34,22 @@
           EVALUATION_SCHEMA = "prob4d.dot-rope-cut3r-native-provider-evaluation"
           SCHEMA_VERSION = 1
           
          +MINIMUM_VALID_MARKERS_PER_FRAME = 3
          +MINIMUM_COMMON_MARKERS_PER_FRAME = 3
          +MARKER_SUPPORT_POLICY: dict[str, object] = {
          +    "policy_version": 2,
          +    "minimum_valid_provider_truth_markers_per_frame": (
          +        MINIMUM_VALID_MARKERS_PER_FRAME
          +    ),
          +    "minimum_common_provider_markers_per_frame": (
          +        MINIMUM_COMMON_MARKERS_PER_FRAME
          +    ),
          +    "rationale": (
          +        "Three finite correspondences are the proper-Sim3 minimum; registered "
          +        "overlap and metric-fit stages aggregate multiple fixed frame groups."
          +    ),
          +}
          +
           
           def _parser() -> argparse.ArgumentParser:
               parser = argparse.ArgumentParser(description=__doc__)
          @@ -124,4 +140,9 @@
                   "window_b": list(range(3, 8)),
               }:
                   raise ValueError("frozen provider windows changed")
          +    evaluation = protocol.get("evaluation")
          +    if not isinstance(evaluation, dict):
          +        raise ValueError("DOT CUT3R evaluation contract is missing")
          +    if evaluation.get("marker_support_policy") != MARKER_SUPPORT_POLICY:
          +        raise ValueError("frozen marker-support policy changed")
               return protocol
          @@ -677,6 +698,10 @@
                   & np.isfinite(coordinates_3d[:count]).all(axis=1)
                   & (sampled_confidence[:, 0] > 0.0)
               )
          -    if np.count_nonzero(valid) < 6:
          -        raise ValueError("fewer than six valid marker samples remain")
          +    valid_count = int(np.count_nonzero(valid))
          +    if valid_count < MINIMUM_VALID_MARKERS_PER_FRAME:
          +        raise ValueError(
          +            f"frame {frame} has {valid_count} valid marker samples; "
          +            f"need at least {MINIMUM_VALID_MARKERS_PER_FRAME}"
          +        )
               marker_indices = np.flatnonzero(valid)
          @@ -708,8 +733,11 @@
                       assume_unique=True,
                       return_indices=True,
                   )
          -        if common.size < 6:
          -            raise ValueError("fewer than six common provider markers remain")
          +        if common.size < MINIMUM_COMMON_MARKERS_PER_FRAME:
          +            raise ValueError(
          +                f"frame {int(frame)} has {common.size} common provider markers; "
          +                f"need at least {MINIMUM_COMMON_MARKERS_PER_FRAME}"
          +            )
                   sources.append(second_points[second_positions])
                   targets.append(first_points[first_positions])
                   groups.append(np.full(common.size, int(frame), dtype=np.int64))
          diff --git a/tests/test_dot_rope_cut3r_native_provider.py b/tests/test_dot_rope_cut3r_native_provider.py
          --- a/tests/test_dot_rope_cut3r_native_provider.py
          +++ b/tests/test_dot_rope_cut3r_native_provider.py
          @@ -6,5 +6,6 @@
           from pathlib import Path
           
           import numpy as np
          +import pytest
           
           from prob4d.dot_rope_cut3r_study import (
          @@ -32,6 +32,15 @@
               assert protocol["reserved_sequences"] == "R04-R70"
               assert protocol["frames"] == list(range(1, 8))
               assert protocol["uncertainty"]["means_held_fixed"] is True
          +    assert protocol["evaluation"]["marker_support_policy"] == {
          +        "policy_version": 2,
          +        "minimum_valid_provider_truth_markers_per_frame": 3,
          +        "minimum_common_provider_markers_per_frame": 3,
          +        "rationale": (
          +            "Three finite correspondences are the proper-Sim3 minimum; registered "
          +            "overlap and metric-fit stages aggregate multiple fixed frame groups."
          +        ),
          +    }
           
           
           def test_request_validator_recomputes_identity(monkeypatch) -> None:
          @@ -87,4 +96,94 @@
               assert valid.tolist() == [True, False]
           
           
          +def _marker_test_run(frames: list[int]) -> dict[str, np.ndarray]:
          +    height = 4
          +    width = 4
          +    rows, columns = np.mgrid[:height, :width]
          +    point_map = np.stack(
          +        (
          +            columns.astype(np.float64),
          +            rows.astype(np.float64),
          +            np.ones((height, width), dtype=np.float64),
          +        ),
          +        axis=-1,
          +    )
          +    return {
          +        "frames": np.asarray(frames, dtype=np.int64),
          +        "points": np.repeat(point_map[None, ...], len(frames), axis=0),
          +        "confidence": np.ones((len(frames), height, width), dtype=np.float64),
          +        "original_sizes": np.repeat(
          +            np.asarray([[width, height]], dtype=np.int64),
          +            len(frames),
          +            axis=0,
          +        ),
          +    }
          +
          +
          +def _three_marker_payload() -> tuple[np.ndarray, np.ndarray]:
          +    coordinates_2d = np.asarray(
          +        [[0.0, 0.0], [1.0, 1.0], [2.0, 2.0]],
          +        dtype=np.float64,
          +    )
          +    coordinates_3d = np.asarray(
          +        [[0.0, 0.0, 1.0], [1.0, 1.0, 1.0], [2.0, 2.0, 1.0]],
          +        dtype=np.float64,
          +    )
          +    return coordinates_2d, coordinates_3d
          +
          +
          +def test_three_valid_markers_are_sufficient_for_one_registered_frame() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1])
          +    coordinates_2d, coordinates_3d = _three_marker_payload()
          +
          +    provider, truth, indices = module._sample_markers(
          +        run,
          +        1,
          +        coordinates_2d,
          +        coordinates_3d,
          +    )
          +
          +    assert provider.shape == (3, 3)
          +    np.testing.assert_array_equal(truth, coordinates_3d)
          +    np.testing.assert_array_equal(indices, np.arange(3))
          +
          +
          +def test_three_common_markers_per_frame_are_sufficient_for_overlap() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1, 2])
          +    coordinates = _three_marker_payload()
          +    frame_payloads = {1: coordinates, 2: coordinates}
          +
          +    source, target, groups = module._collect_pair(
          +        run,
          +        run,
          +        frame_payloads,
          +        [1, 2],
          +    )
          +
          +    assert source.shape == (6, 3)
          +    np.testing.assert_array_equal(source, target)
          +    np.testing.assert_array_equal(groups, [1, 1, 1, 2, 2, 2])
          +
          +
          +def test_fewer_than_three_valid_markers_fail_with_a_counted_reason() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1])
          +    coordinates_2d = np.asarray(
          +        [[0.0, 0.0], [1.0, 1.0], [-1.0, -1.0]],
          +        dtype=np.float64,
          +    )
          +    coordinates_3d = np.asarray(
          +        [[0.0, 0.0, 1.0], [1.0, 1.0, 1.0], [2.0, 2.0, 1.0]],
          +        dtype=np.float64,
          +    )
          +
          +    with pytest.raises(
          +        ValueError,
          +        match=r"frame 1 has 2 valid marker samples; need at least 3",
          +    ):
          +        module._sample_markers(run, 1, coordinates_2d, coordinates_3d)
          +
          +
           def test_registered_covariance_closures_are_psd_and_fixed_dimension() -> None:
          diff --git a/protocols/dot-rope-cut3r-native-provider-v1.json b/protocols/dot-rope-cut3r-native-provider-v1.json
          --- a/protocols/dot-rope-cut3r-native-provider-v1.json
          +++ b/protocols/dot-rope-cut3r-native-provider-v1.json
          @@ -1,10 +1,16 @@
           {
             "archive": "R01-10.zip",
             "camera": "cam001",
          -  "claim_boundary": "Source-development marker-free-provider evidence on R01-R03 only. R04-R70 remain unopened. This experiment does not establish held-out transfer, real uncertainty calibration, BayesianPhysTwin benefit, Causal4D benefit, safety, or state of the art.",
          +  "claim_boundary": "Source-development marker-free-provider evidence on R01-R03 only. The per-frame marker-support threshold was amended after source run 33329701704 exposed an unregistered six-marker redundancy requirement; R04-R70 remain unopened and did not inform the amendment. This experiment does not establish held-out transfer, real uncertainty calibration, BayesianPhysTwin benefit, Causal4D benefit, safety, or state of the art.",
             "dataset_doi": "10.13021/ORC2020/XXLVXM",
             "evaluation": {
               "marker_sampling": "bilinear-output-grid-from-original-image-coordinates",
          +    "marker_support_policy": {
          +      "minimum_common_provider_markers_per_frame": 3,
          +      "minimum_valid_provider_truth_markers_per_frame": 3,
          +      "policy_version": 2,
          +      "rationale": "Three finite correspondences are the proper-Sim3 minimum; registered overlap and metric-fit stages aggregate multiple fixed frame groups."
          +    },
               "metric_fit_a_frames": [
                 1,
                 2
          @@ -51,7 +57,7 @@
               "continuous versus identity and estimated-Sim3 restarted stitching RMSE divided by rope span",
               "fixed-mean normalized joint Gaussian NLL for eight registered uncertainty closures"
             ],
          -  "protocol_id": "af6528f54699f3c9fb185764b029acd897a52e242e799d317e42710ee0f21c2c",
          +  "protocol_id": "7d7e8a272c892d5646476fa0f97b8def31ad691e286bfac880c156fffa3f59fb",
             "provider": {
               "device": "cuda",
               "image_size": 512,
          PATCH
          git apply <<'PATCH'
          diff --git a/scripts/science/run_dot_rope_cut3r_native_provider.py b/scripts/science/run_dot_rope_cut3r_native_provider.py
          --- a/scripts/science/run_dot_rope_cut3r_native_provider.py
          +++ b/scripts/science/run_dot_rope_cut3r_native_provider.py
          @@ -34,6 +34,22 @@
           EVALUATION_SCHEMA = "prob4d.dot-rope-cut3r-native-provider-evaluation"
           SCHEMA_VERSION = 1
           
          +MINIMUM_VALID_MARKERS_PER_FRAME = 3
          +MINIMUM_COMMON_MARKERS_PER_FRAME = 3
          +MARKER_SUPPORT_POLICY: dict[str, object] = {
          +    "policy_version": 2,
          +    "minimum_valid_provider_truth_markers_per_frame": (
          +        MINIMUM_VALID_MARKERS_PER_FRAME
          +    ),
          +    "minimum_common_provider_markers_per_frame": (
          +        MINIMUM_COMMON_MARKERS_PER_FRAME
          +    ),
          +    "rationale": (
          +        "Three finite correspondences are the proper-Sim3 minimum; registered "
          +        "overlap and metric-fit stages aggregate multiple fixed frame groups."
          +    ),
          +}
          +
           
           def _parser() -> argparse.ArgumentParser:
               parser = argparse.ArgumentParser(description=__doc__)
          @@ -124,4 +140,9 @@
                   "window_b": list(range(3, 8)),
               }:
                   raise ValueError("frozen provider windows changed")
          +    evaluation = protocol.get("evaluation")
          +    if not isinstance(evaluation, dict):
          +        raise ValueError("DOT CUT3R evaluation contract is missing")
          +    if evaluation.get("marker_support_policy") != MARKER_SUPPORT_POLICY:
          +        raise ValueError("frozen marker-support policy changed")
               return protocol
          @@ -677,6 +698,10 @@
                   & np.isfinite(coordinates_3d[:count]).all(axis=1)
                   & (sampled_confidence[:, 0] > 0.0)
               )
          -    if np.count_nonzero(valid) < 6:
          -        raise ValueError("fewer than six valid marker samples remain")
          +    valid_count = int(np.count_nonzero(valid))
          +    if valid_count < MINIMUM_VALID_MARKERS_PER_FRAME:
          +        raise ValueError(
          +            f"frame {frame} has {valid_count} valid marker samples; "
          +            f"need at least {MINIMUM_VALID_MARKERS_PER_FRAME}"
          +        )
               marker_indices = np.flatnonzero(valid)
          @@ -708,8 +733,11 @@
                       assume_unique=True,
                       return_indices=True,
                   )
          -        if common.size < 6:
          -            raise ValueError("fewer than six common provider markers remain")
          +        if common.size < MINIMUM_COMMON_MARKERS_PER_FRAME:
          +            raise ValueError(
          +                f"frame {int(frame)} has {common.size} common provider markers; "
          +                f"need at least {MINIMUM_COMMON_MARKERS_PER_FRAME}"
          +            )
                   sources.append(second_points[second_positions])
                   targets.append(first_points[first_positions])
                   groups.append(np.full(common.size, int(frame), dtype=np.int64))
          diff --git a/tests/test_dot_rope_cut3r_native_provider.py b/tests/test_dot_rope_cut3r_native_provider.py
          --- a/tests/test_dot_rope_cut3r_native_provider.py
          +++ b/tests/test_dot_rope_cut3r_native_provider.py
          @@ -6,5 +6,6 @@
           from pathlib import Path
           
           import numpy as np
          +import pytest
           
           from prob4d.dot_rope_cut3r_study import (
          @@ -32,6 +32,15 @@
               assert protocol["reserved_sequences"] == "R04-R70"
               assert protocol["frames"] == list(range(1, 8))
               assert protocol["uncertainty"]["means_held_fixed"] is True
          +    assert protocol["evaluation"]["marker_support_policy"] == {
          +        "policy_version": 2,
          +        "minimum_valid_provider_truth_markers_per_frame": 3,
          +        "minimum_common_provider_markers_per_frame": 3,
          +        "rationale": (
          +            "Three finite correspondences are the proper-Sim3 minimum; registered "
          +            "overlap and metric-fit stages aggregate multiple fixed frame groups."
          +        ),
          +    }
           
           
           def test_request_validator_recomputes_identity(monkeypatch) -> None:
          @@ -87,4 +96,94 @@
               assert valid.tolist() == [True, False]
           
           
          +def _marker_test_run(frames: list[int]) -> dict[str, np.ndarray]:
          +    height = 4
          +    width = 4
          +    rows, columns = np.mgrid[:height, :width]
          +    point_map = np.stack(
          +        (
          +            columns.astype(np.float64),
          +            rows.astype(np.float64),
          +            np.ones((height, width), dtype=np.float64),
          +        ),
          +        axis=-1,
          +    )
          +    return {
          +        "frames": np.asarray(frames, dtype=np.int64),
          +        "points": np.repeat(point_map[None, ...], len(frames), axis=0),
          +        "confidence": np.ones((len(frames), height, width), dtype=np.float64),
          +        "original_sizes": np.repeat(
          +            np.asarray([[width, height]], dtype=np.int64),
          +            len(frames),
          +            axis=0,
          +        ),
          +    }
          +
          +
          +def _three_marker_payload() -> tuple[np.ndarray, np.ndarray]:
          +    coordinates_2d = np.asarray(
          +        [[0.0, 0.0], [1.0, 1.0], [2.0, 2.0]],
          +        dtype=np.float64,
          +    )
          +    coordinates_3d = np.asarray(
          +        [[0.0, 0.0, 1.0], [1.0, 1.0, 1.0], [2.0, 2.0, 1.0]],
          +        dtype=np.float64,
          +    )
          +    return coordinates_2d, coordinates_3d
          +
          +
          +def test_three_valid_markers_are_sufficient_for_one_registered_frame() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1])
          +    coordinates_2d, coordinates_3d = _three_marker_payload()
          +
          +    provider, truth, indices = module._sample_markers(
          +        run,
          +        1,
          +        coordinates_2d,
          +        coordinates_3d,
          +    )
          +
          +    assert provider.shape == (3, 3)
          +    np.testing.assert_array_equal(truth, coordinates_3d)
          +    np.testing.assert_array_equal(indices, np.arange(3))
          +
          +
          +def test_three_common_markers_per_frame_are_sufficient_for_overlap() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1, 2])
          +    coordinates = _three_marker_payload()
          +    frame_payloads = {1: coordinates, 2: coordinates}
          +
          +    source, target, groups = module._collect_pair(
          +        run,
          +        run,
          +        frame_payloads,
          +        [1, 2],
          +    )
          +
          +    assert source.shape == (6, 3)
          +    np.testing.assert_array_equal(source, target)
          +    np.testing.assert_array_equal(groups, [1, 1, 1, 2, 2, 2])
          +
          +
          +def test_fewer_than_three_valid_markers_fail_with_a_counted_reason() -> None:
          +    module = _load_script()
          +    run = _marker_test_run([1])
          +    coordinates_2d = np.asarray(
          +        [[0.0, 0.0], [1.0, 1.0], [-1.0, -1.0]],
          +        dtype=np.float64,
          +    )
          +    coordinates_3d = np.asarray(
          +        [[0.0, 0.0, 1.0], [1.0, 1.0, 1.0], [2.0, 2.0, 1.0]],
          +        dtype=np.float64,
          +    )
          +
          +    with pytest.raises(
          +        ValueError,
          +        match=r"frame 1 has 2 valid marker samples; need at least 3",
          +    ):
          +        module._sample_markers(run, 1, coordinates_2d, coordinates_3d)
          +
          +
           def test_registered_covariance_closures_are_psd_and_fixed_dimension() -> None:
          diff --git a/protocols/dot-rope-cut3r-native-provider-v1.json b/protocols/dot-rope-cut3r-native-provider-v1.json
          --- a/protocols/dot-rope-cut3r-native-provider-v1.json
          +++ b/protocols/dot-rope-cut3r-native-provider-v1.json
          @@ -1,10 +1,16 @@
           {
             "archive": "R01-10.zip",
             "camera": "cam001",
          -  "claim_boundary": "Source-development marker-free-provider evidence on R01-R03 only. R04-R70 remain unopened. This experiment does not establish held-out transfer, real uncertainty calibration, BayesianPhysTwin benefit, Causal4D benefit, safety, or state of the art.",
          +  "claim_boundary": "Source-development marker-free-provider evidence on R01-R03 only. The per-frame marker-support threshold was amended after source run 33329701704 exposed an unregistered six-marker redundancy requirement; R04-R70 remain unopened and did not inform the amendment. This experiment does not establish held-out transfer, real uncertainty calibration, BayesianPhysTwin benefit, Causal4D benefit, safety, or state of the art.",
             "dataset_doi": "10.13021/ORC2020/XXLVXM",
             "evaluation": {
               "marker_sampling": "bilinear-output-grid-from-original-image-coordinates",
          +    "marker_support_policy": {
          +      "minimum_common_provider_markers_per_frame": 3,
          +      "minimum_valid_provider_truth_markers_per_frame": 3,
          +      "policy_version": 2,
          +      "rationale": "Three finite correspondences are the proper-Sim3 minimum; registered overlap and metric-fit stages aggregate multiple fixed frame groups."
          +    },
               "metric_fit_a_frames": [
                 1,
                 2
          @@ -51,7 +57,7 @@
               "continuous versus identity and estimated-Sim3 restarted stitching RMSE divided by rope span",
               "fixed-mean normalized joint Gaussian NLL for eight registered uncertainty closures"
             ],
          -  "protocol_id": "af6528f54699f3c9fb185764b029acd897a52e242e799d317e42710ee0f21c2c",
          +  "protocol_id": "7d7e8a272c892d5646476fa0f97b8def31ad691e286bfac880c156fffa3f59fb",
             "provider": {
               "device": "cuda",
               "image_size": 512,
          PATCH
          git rm .github/workflows/apply-dot-cut3r-marker-support-v2.yml
          git diff --check
          git status --short
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            scripts/science/run_dot_rope_cut3r_native_provider.py \
            tests/test_dot_rope_cut3r_native_provider.py \
            protocols/dot-rope-cut3r-native-provider-v1.json
          git commit -m "Repair DOT CUT3R marker-support evaluator"
          git push origin HEAD:fix/dot-cut3r-marker-support-v2
