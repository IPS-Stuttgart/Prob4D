name: DOT CUT3R gpuserver6000 bootstrap v3

on:
  push:
    branches:
      - ops/dot-cut3r-gpuserver6000-preflight-v1
    paths:
      - ".github/workflows/dot-cut3r-gpuserver6000-bootstrap-v3.yml"

permissions:
  contents: read

concurrency:
  group: dot-cut3r-gpuserver6000-bootstrap-v3
  cancel-in-progress: true

jobs:
  release:
    name: Release gpuserver6000 for frozen held-out confirmation
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The diagnostic bootstrap was superseded by the authoritative R04-R10 workflow."
