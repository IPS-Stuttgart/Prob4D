### Added

- Added a source-frozen, held-out DEFORM DLO4/DLO5 evaluation of query-aware
  use of rank-deficient `Sim(3)` factors. Across all 28 official evaluation
  trajectories, the method admitted every gauge-insensitive segment-centroid
  query and returned the complete fallback for every gauge-sensitive off-axis
  query. The retained evidence explicitly records that accepted centroid
  covariance was underdispersed and that the experiment tests controlled gauges
  on real geometry rather than learned visual-provider competence.
