# Frozen DOT R11–R30 execution on gpuserver6000

Add a second control plane for the already frozen DOT R11–R30 query-selective CUT3R experiment. The new workflow consumes the exact same protocol and evaluator, but binds the learned-provider job to `gpuserver6000` / `workstation2` and to the separately attested prewarmed runtime and compressed archives.

The scientific cohort, reserve, methods, thresholds, query definitions, comparators, bootstrap, terminal criteria, prerequisite, and prediction-before-outcome information order are unchanged. R31–R70 remain unopened, and no target-side tuning, BayesianPhysTwin execution, or Causal4D execution is authorized.
