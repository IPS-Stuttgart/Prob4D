### Experimental scientific validation

- Add a separately versioned PokeFlex real-geometry diagnostic for the
  posterior-preserving shared-noise compression kernel.
- Inventory a known-incomplete ZIP mirror without treating archive count as
  release completeness, and fail closed on unsupported or identity-ambiguous
  geometry.
- Compare the full local-Gaussian query posterior with posterior-preserving,
  equal-rank covariance-PCA, conditional-only, and cached-query arms.
- This does not reopen the retained Causal4D PokeFlex target, promote a provider,
  change calibration, or establish real physical or causal benefit.
