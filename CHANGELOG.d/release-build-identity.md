Add a generated, package-byte-bound build identity to wheels and source
distributions, use it for exact installed-runtime revision attestation, and make
untracked source-checkout files invalidate claim-bearing execution.

This is software provenance and release-integrity infrastructure only. It changes
no estimator, calibration, provider decision, target-access rule, fallback, or
scientific result.
