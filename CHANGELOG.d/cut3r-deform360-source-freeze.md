- Add a target-closed Deform360 source-freeze protocol and self-hosted workflow
  for the existing CUT3R native-versus-Prob4D comparison. The stage validates
  the exact ten source object sessions, retains all twelve forbidden confirmation
  sessions, chooses a deterministic geometry-balanced four-camera panel from
  source support metadata, binds the exact CUT3R checkpoint and Prob4D wheel,
  and emits the canonical comparison specification without decoding RGB frames
  or opening predictions, residuals, future geometry, or target payloads.
