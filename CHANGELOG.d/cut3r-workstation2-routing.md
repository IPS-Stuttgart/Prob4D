Route the exact retained CUT3R Deform360 source-freeze retry to the existing `host-workstation2` retained-data runner and fail closed unless the accepted runner is exactly `workstation2` on Linux/X64 with `nvidia-smi` available. This changes execution routing only; the frozen historical revision, request, provider, cohort, checkpoint policy, source/target boundaries, and scientific decision rules are unchanged.
