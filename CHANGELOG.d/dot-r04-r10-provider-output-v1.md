# Fixed

- Permit the frozen DOT R04–R10 wrapper to remove only a pre-created empty provider directory before the hash-pinned predictor performs its exclusive creation. Nonempty, file, and symbolic-link outputs still fail closed; all scientific inputs remain unchanged.
