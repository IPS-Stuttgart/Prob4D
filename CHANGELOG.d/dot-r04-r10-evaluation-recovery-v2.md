# Fixed

- Add a hosted-only recovery for the frozen DOT R04–R10 marker evaluation. It reuses the exact sealed CUT3R provider artifact from run `33500239597`, verifies its GitHub digest, bundle seal, request identity, files, and information boundary, and then executes the unchanged evaluator without pre-creating its exclusive output directory. The provider is not rerun, no scientific parameter is changed, and R11–R70 remain unopened.
