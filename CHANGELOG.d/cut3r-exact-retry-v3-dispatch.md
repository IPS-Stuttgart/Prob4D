- Add a v3 exact CUT3R retry dispatcher that admits only the registered 3,106-byte failure-diagnostic artifact from the historical zero-result run. It binds the artifact ID, digest, size, run, retained-data job, and step outcomes; rejects any additional or changed artifact; prevents duplicate target dispatches; and leaves every scientific input and outcome boundary unchanged.
- Rebind the dispatcher authorization check to the existing `host-workstation2` retained-data runner and require the target workflow to fail closed on exact `workstation2`, Linux/X64, and `nvidia-smi` runtime checks. This changes routing authorization only and leaves the frozen historical request unchanged.
- Permit one replacement of the exact retry run `32764290533` only after verifying its exact cancelled workflow identity, complete hosted/preflight job roster, successful 20-minute watchdog cancellation step, zero retained execute steps, and zero Actions artifacts. Any newer retry remains duplicate-protected and no scientific input or outcome boundary changes.
