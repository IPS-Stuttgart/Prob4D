Add an experimental query-preserving shared-covariance compressor. It selects a
deterministic whole-eigenspace latent subspace under frozen observation-trace and
per-query trace/spectral loss limits, and fails closed to the exact full factor
when no admissible reduction fits the requested rank budget.
