- Added a content-addressed CUT3R source-competence lock and strict paired-record
  aggregator that produces the existing equal-group source mean and
  identity/reliability decision. It rejects omitted frozen frames, seeds, arms,
  cases, or groups; retains predeclared complete-group technical failures; keeps
  target access closed; and exposes freeze, replay, summary, and fail-on-negative
  grouped commands.
